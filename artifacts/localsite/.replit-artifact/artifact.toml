kind = "web"
previewPath = "/"
title = "LocalSite"
version = "1.0.0"
id = "artifacts/localsite"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 20529
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/localsite run dev"

[services.env]
PORT = "20529"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/localsite", "run", "build"]
publicDir = "artifacts/localsite/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"
